class Mac{

    constructor(deviceName, mac, transportId, path){

        this.deviceName = deviceName;
        this.mac = mac;
        this.transportId = transportId;
        this.path = path
    }
}

module.exports = Mac;